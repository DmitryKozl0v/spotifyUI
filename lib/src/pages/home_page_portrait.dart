import 'package:flutter/material.dart';

Widget portraitHomePage(){
  return Container(
    child: Text('Portrait page!'),
    decoration: BoxDecoration(
      color: Colors.teal,
      borderRadius: BorderRadius.circular(15.0)
    ),
  );
}